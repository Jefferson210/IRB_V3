<%= form_for(@irb_selections_picture) do |f| %>
  <% if @irb_selections_picture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@irb_selections_picture.errors.count, "error") %> prohibited this irb_selections_picture from being saved:</h2>

      <ul>
      <% @irb_selections_picture.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
